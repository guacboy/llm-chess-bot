# PyTorch - install manually with GPU support before running pip install -r requirements.txt:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
torch>=2.7.0
torchvision>=0.22.0
torchaudio>=2.7.0

# Chess game logic
python-chess>=1.999

# Numerical computing
numpy>=2.0.0

# Progress bars
tqdm>=4.66.0

# Web server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Loads API token from .env file
python-dotenv>=1.0.0

# Stockfish chess engine (not a pip package - must be installed manually):
#   1. Download the Windows binary from https://stockfishchess.org/download/
#   2. Extract and place the .exe into src/stockfish/
#      e.g. src/stockfish/stockfish-windows-x86-64-avx2.exe
#   The bot auto-detects any .exe inside that folder at startup.
